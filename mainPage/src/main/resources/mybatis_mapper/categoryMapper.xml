<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.mainPage.mapper.CategoryMapper">

	<resultMap type="Category" id="CategoryMap">
		<id column="categoryid" property="categoryId"/>
		<result column="categoryName" property="categoryName"/>
		<collection property="product" javaType="java.util.ArrayList" ofType="Product">
			<id column="c_productid" property="productid"/>
			<result column="c_productName" property="productName"/>
			<result column="c_productSize" property="productSize"/>
			<result column="c_productColor" property="productColor"/>
			<result column="c_productGroup" property="productGroup"/>
			<result column="c_productInfo" property="productInfo"/>
			<result column="c_productStock" property="productStock"/>
			<result column="c_productDate" property="productDate"/>
			<result column="c_price" property="price"/>
			<result column="c_categoryid" property="categoryId"/>
		</collection>
	</resultMap>
	
	
	<select id="selectCategoryAll" resultType="Category">
		SELECT 
			c.categoryid as 'category.categoryId',
			c.categoryName as 'category.categoryName', 
			p.productid as 'product.productid',
			p.productName as 'product.productName',
			p.price as 'product.price',
			p.categoryid as 'product.category.categoryid'
			FROM CATEGORY c LEFT JOIN PRODUCT p 
			USING (categoryId)
			LIMIT #{startRow}, #{pageSize} 
			
		<!-- SELECT *,categoryid as 'category.categoryId'FROM PRODUCT LIMIT #{startRow},#{pageSize} -->
	</select>

	<select id="selectAllCount" resultType="int">
		SELECT COUNT(*) FROM PRODUCT
	</select>
	
	<select id="selectOne" resultMap="CategoryMap">
		SELECT * FROM CATEGORY WHERE categoryid=#{categoryid}
	</select>
	
</mapper>